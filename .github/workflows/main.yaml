name: Linux-TKG Silvermont Hyper-Light

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  tkg-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar Dependencias (Basado en README)
        run: |
          sudo apt-get update
          sudo apt-get install -y git build-essential libncurses-dev flex bison openssl \
            libssl-dev dkms libelf-dev libudev-dev libpci-dev libiberty-dev autoconf \
            xz-utils zstd bc ccache python3 kmod fakeroot

      - name: Cache Kernel Sources
        uses: actions/cache@v4
        with:
          path: linux-tkg/linux-src-git
          key: linux-tkg-src-6.6
          restore-keys: linux-tkg-src-

      - name: Preparar linux-tkg
        run: |
          git clone --depth 1 https://github.com/Frogging-Family/linux-tkg.git
          cp customization.cfg linux-tkg/

      - name: Compilar
        run: |
          cd linux-tkg
          # Usamos 'config' primero para preparar y luego 'install' para empaquetar
          ./install.sh script
        env:
          _no_confirm: "true"

      - name: Capturar Paquete
        run: |
          mkdir -p release_files
          # TKG en modo Arch genera un .pkg.tar.zst
          find linux-tkg -name "*.pkg.tar.zst" -exec cp {} release_files/ \;
          # Si no hizo el paquete, buscamos el binario
          find linux-tkg/linux-src-git -name "vmlinuz*" -exec cp {} release_files/ \;

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: tkg-6.6-silvermont-${{ github.run_number }}
          name: Kernel Linux-TKG 6.6 LTS (Silvermont Optimized)
          body: |
            ## 🚀 Kernel Personalizado para Intel Silvermont (N2800/N2807)
            
            Este kernel ha sido compilado específicamente para maximizar la fluidez en hardware limitado.
            
            ### 🛠 Detalles Técnicos:
            - **Versión:** 6.6 LTS (Long Term Support)
            - **Arquitectura:** Optimizado para `Silvermont` (Celeron/Atom)
            - **Scheduler:** `PDS` (Prioriza la respuesta del escritorio e i3)
            - **Compresión:** `LZ4` (El arranque más rápido posible)
            - **Estado:** "Hyper-Light" (Sin drivers innecesarios, sin debug symbols)
            
            ### 🐧 Compatibilidad:
            - **Distro:** Diseñado para **Arch Linux** y derivados (EndeavourOS, Manjaro).
            - **Formato:** Paquete `.pkg.tar.zst` (Instalación nativa con Pacman).
            
            ### 📦 Instrucciones de Instalación:
            1. Descargá el archivo `.pkg.tar.zst` de este Release.
            2. Abrí una terminal en la carpeta de descarga.
            3. Instalá usando pacman:
               ```bash
               sudo pacman -U linux-tkg-silvermont-6.6.x.pkg.tar.zst
               ```
            4. Actualizá tu cargador de arranque (GRUB o systemd-boot).
            5. Reiniciá y seleccioná el nuevo kernel en el menú.
            
            ---
            *Compilado automáticamente vía GitHub Actions.*
          files: release_files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
